services:
  - type: web
    name: askoxy-single
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python single_server.py"
    envVars:
      - key: PORT
        fromService:
          type: web
          name: askoxy-single
          property: port