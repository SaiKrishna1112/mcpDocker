services:
  - type: web
    name: askoxy-unified
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python unified_server.py"
    envVars:
      - key: PORT
        fromService:
          type: web
          name: askoxy-unified
          property: port