services:
  - type: web
    name: oxyloans-unified
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python unified_server.py"
    envVars:
      - key: PORT
        fromService:
          type: web
          name: oxyloans-unified
          property: port